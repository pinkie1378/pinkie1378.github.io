{
    "build": {
        "dockerfile": "Dockerfile"
    },
    "forwardPorts": [
        4000
    ],
    "mounts": [
        {
            "type": "bind",
            "source": "${localWorkspaceFolder}",
            "target": "/home/app"
        }
    ]
}